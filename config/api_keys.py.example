# API配置
# 注意：现在不再使用服务器配置的API Key，所有用户必须在前端设置中配置自己的API Key
# 此文件仅用于配置模型名称和其他非敏感配置项

# Gemini API配置（仅模型配置，不再使用API Key）
# GEMINI_API_KEY = None  # 已禁用，用户必须在前端配置自己的API Key
GEMINI_MODEL = "gemini-2.5-flash-image"

# 豆包API配置（仅模型配置，不再使用API Key）
# DOUBAO_API_KEY = None  # 已禁用，用户必须在前端配置自己的API Key
DOUBAO_MODEL = "doubao-seedream-4-0-250828"
DOUBAO_WATERMARK = False  # 是否添加水印，False=不添加，True=添加"AI生成"水印

# 文件存储配置
UPLOAD_FOLDER = "uploads"
RESULT_FOLDER = "results"
MAX_FILE_SIZE = 10 * 1024 * 1024  # 10MB

# 允许的文件类型
ALLOWED_EXTENSIONS = {'png', 'jpg', 'jpeg', 'gif', 'webp'}

# API选择配置
DEFAULT_API = "gemini"  # 默认使用Gemini
SUPPORTED_APIS = ["gemini", "doubao"]

